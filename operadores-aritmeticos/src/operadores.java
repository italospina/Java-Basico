public class operadores {
    /*

        1. Operadores Aritméticos (Usados para cálculos matemáticos)
        Operador	Descrição	Exemplo
        +	Adição	int soma = 10 + 5;
        -	Subtração	int sub = 10 - 5;
        *	Multiplicação	int mult = 10 * 5;
        /	Divisão	int div = 10 / 5;
        %	Módulo (Resto da divisão)	int resto = 10 % 3; // 1

        2. Operadores Relacionais (Comparação entre valores)
        Operador	Descrição	Exemplo
        ==	Igual a	x == y
        !=	Diferente de	x != y
        >	Maior que	x > y
        <	Menor que	x < y
        >=	Maior ou igual a	x >= y
        <=	Menor ou igual a	x <= y

        3. Operadores Lógicos (Usados para expressões booleanas)
        Operador	Descrição	Exemplo
        &&	E lógico	(x > 5 && y < 10)
        `		`
        !	Negação lógica	!(x > 5)

        4. Operadores de Atribuição (Usados para atribuir valores a variáveis)
        Operador	Descrição	Exemplo
        =	Atribuição simples	int x = 10;
        +=	Adição e atribuição	x += 5; // x = x + 5
        -=	Subtração e atribuição	x -= 5; // x = x - 5
        *=	Multiplicação e atribuição	x *= 5; // x = x * 5
        /=	Divisão e atribuição	x /= 5; // x = x / 5
        %=	Módulo e atribuição	x %= 5; // x = x % 5

        5. Operadores de Incremento e Decremento (Aumentam ou diminuem valores de variáveis)
        Operador	Descrição	Exemplo
        ++	Incremento	x++; // x = x + 1
        --	Decremento	x--; // x = x - 1

        6. Operador Ternário (Substitui if-else simples)
        java
        Copiar
        Editar
        int idade = 18;
        String resultado = (idade >= 18) ? "Maior de idade" : "Menor de idade";
     */
}
